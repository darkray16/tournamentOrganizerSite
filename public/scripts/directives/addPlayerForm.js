angular.module('setupNewTournament')
  .directive('addPlayerForm', function() {
    return {
      link: function(scope, element, attrs) {
        scope.addPlayer = function() {
          var name = scope.playerName,
              notes = scope.playerNotes;
          scope.
        }
      }
    };
  })